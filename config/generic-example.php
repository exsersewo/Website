<?php
    $clientID = 0; //Discord ClientID
    $isProd = true; //used for endpoints and other things
    $enabledProfile = false; //Used to denote if profile should be enabled
    $enabledDashboard = false; //Used to denote if dashboard should be enabled
	$isDev = false; //Used to denote if unstable Website
?>